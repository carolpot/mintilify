---
title: "Changelog"
description: "Recent updates to the Lusha API, data, and MCP server."
---

<!-- VERIFY: This file was originally scoped to "May 2026" but the live changelog at docs.lusha.com/changelog has no entries dated April or May 2026 — the real timeline jumps from March 10, 2026 straight to June 2, 2026. Entries below are pulled from the live changelog (Sept 2025 through June 2026, newest first). The two original May 2026 entries are kept further down but flagged — they aren't corroborated by the live changelog. -->

## July 15, 2026

### New: V3 is now the default API

The V3 API is now the canonical version of the Lusha API and is documented as the default in these docs. V2 remains fully supported as **Legacy** — pick a version at the top of the sidebar to browse either surface. See the [V3 introduction](/introduction) or the [V3 migration guide](https://docs.lusha.com/tutorials/v3-migration-guide) to plan your move.

### New: Unified search-then-enrich flow

Every V3 [enrichment endpoint](/enrichment/overview) now follows the same **search, then enrich** pattern, and each endpoint accepts up to **100 contacts or companies in a single call** — no more separate single-record vs. bulk endpoints. Preview a match (and its credit cost) with Search, then reveal full data with Enrich, or run both in one request with **Search and Enrich**.

### New: Unified filter discovery endpoints

Filter discovery is now handled by two generic endpoints per resource instead of one endpoint per filter type. Use `GET /v3/companies/prospecting/filters` and `GET /v3/companies/prospecting/filters/{filterType}/values` (and the equivalent contact endpoints) to discover every filter type and fetch its valid values. See [company filters](/filters/company-filters) and [contact filters](/filters/contact-filters).

### Updated: Lusha MCP server is now generally available

The [Lusha MCP server](/mcp/overview) at `https://mcp.lusha.com` has moved out of beta and is generally available. Connect it to Claude, ChatGPT, Codex, Cursor, VS Code, Gemini CLI, n8n, and other MCP-compatible clients via OAuth or API key to run enrichment, prospecting, signals, decision-maker, and website-visit workflows from natural language.

### Beta: Decision Makers and Website Visits

The [Decision Makers](/decision-makers/overview) (`POST /v3/contacts/decision-makers`) and [Website Visits](/website-visits/overview) (`POST /v3/companies/website-visits`) endpoints are available in **beta**. Request and response shapes may change before general availability.

## June 8, 2026

### New: Decision Makers API

New endpoint `POST /v3/contacts/decision-makers` resolves a company to its most relevant contacts. Supply a company by domain or Lusha company ID and get back a ranked list of decision-maker contacts with name, title, location, and LinkedIn data.

## June 2, 2026

### New: Website Visits API

New endpoint `POST /v3/companies/website-visits` identifies companies visiting your tracked domains. Supports filtering by engagement score band (cold/warm/hot), session metrics, and geography, with a maximum lookback of 3 months.

## March 10, 2026

### New: Signal sub-filters and company lookalikes V3

Added an optional `signalsFilters` parameter to bulk company enrichment and the signals APIs. Launched `POST /v3/companies/lookalike` and a webhooks opt-out subscription feature.

<Note>
  In Contact Lookalikes V3, the result field `id` was renamed to `contactId`.
</Note>

## February 26, 2026

### New: Contact Lookalikes V3 and signal rate control

`POST /v3/lookalike/contacts` replaces `/api/recommendations/contacts` and supports multiple seed types. All four signals endpoints now require a `maxResultsPerSignal` parameter to control response size.

## February 18, 2026

### New: Company data points and expanded news signals

Added 7 new company fields, including `linkedinFollowers` and a `companyLocations` array. Replaced the generic `newsEvent` signal category with 7 granular news signal types across the API and webhooks.

## January 26, 2026

### New: Webhooks API

Launched the webhooks system: subscription management endpoints, real-time signal notifications, HMAC-SHA256 signature verification, automatic retry with exponential backoff (up to 3 attempts), and 90–180 day audit log retention.

## January 5, 2026

### Updated: Prospecting signal filtering

Prospecting search endpoints now support signal-based filtering and a `startDate` parameter for time-based filtering. Responses include a `signalTypes` array per result.

<!-- VERIFY: source listed this date as "January 5, 2025", which would place it out of sequence with 1.7.0 (Jan 26, 2026) just above. Kept as originally dated pending confirmation with Eng — may be a typo for 2026 on the live site. -->

## October 15, 2025

### New: Recommendations API

Introduced AI-powered recommendation endpoints for contacts and companies to help expand prospect lists. Later superseded by Lookalikes V3.

## September 30, 2025

### New: Free-text search

Added a `searchText` filter supporting natural-language queries across the contact and company prospecting APIs.

## September 10, 2025

### New: Signals API expansion

Added four new signals endpoints, plus a GET endpoint for available signal filter types. Added a `partialProfile` parameter for simplified contact profiles.

---

<!-- VERIFY: The two entries below predate this migration and are not confirmed by the live changelog at docs.lusha.com/changelog (which has no April/May 2026 entries at all). Keeping them pending confirmation with Eng/Product rather than deleting outright — flag for removal or correction if they can't be sourced. -->

## May 12, 2026

### New: MCP server (beta)

<!-- VERIFY: The live docs.lusha.com/mcp-docs page now shows the MCP server as generally available (OAuth support for Claude/ChatGPT/Codex, 20+ tools), not "beta" — and this launch isn't listed on the live changelog. If this entry is kept, it likely needs a follow-up "MCP server: generally available" entry dated to the real GA date. -->

The Lusha MCP server is now in public beta. Connect Lusha data to Claude, Cursor, Windsurf, and any other MCP-compatible client. See the [MCP documentation](/mcp/overview) to get started.

### Fixed: API Reference endpoint badges

All API Reference pages now correctly display the HTTP method badge and interactive playground. Previously, enrichment, signal, lookalike, and webhook pages were missing the method indicator.

## May 1, 2026

### Updated: Bulk company endpoint path

The bulk company enrichment endpoint is now documented at its correct path, `POST /bulk/company/v2`. The OpenAPI spec previously listed this operation under `POST /v2/company`.

<Note>
  This was a documentation fix only — the API endpoint itself did not change.
</Note>
